name: Run Tests
on:
  push:
    branches:
      - master
jobs:
  test:
    runs-on: ubuntu-latest
    container: node:14.16
    
    steps:
      - name: checkout
        uses: actions/checkout@v2

      - name: node js exists check
        run: node -v
        
      - name: node js exists check
        run: node -v
